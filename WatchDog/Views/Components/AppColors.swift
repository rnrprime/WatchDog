import SwiftUI

extension Color {
    static let accentTeal = Color(red: 0.06, green: 0.43, blue: 0.34)
    static let accentTealLight = Color(red: 0.88, green: 0.96, blue: 0.93)
    static let statusValid = Color.green
    static let statusExpiring = Color.orange
    static let statusExpired = Color.red
}
